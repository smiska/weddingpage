// GULPFILE.JS CONFIGURATION

// include gulp plugins
var 
    gulp = require("gulp");

// file locations 

var
    source = "source/",
    destination = "build/";

// gulp default task

gulp.task("default", function(){

});
